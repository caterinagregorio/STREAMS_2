# STREAMS
Semi-supervised Trajectory REconstruction Across interval-censored Multi-State models

To install the package run the following instruction: 

install.packages("remotes")
remotes::install_github("Alepescinaa/STREAMS", ref = "master")

